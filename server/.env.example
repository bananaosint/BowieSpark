# Resolves relative to server/prisma/schema.prisma -> server/prisma/dev.db
DATABASE_URL="file:./dev.db"
PORT=4000
NODE_ENV=development
# Dev-only auth shim. MUST be false before any real deployment.
DEV_AUTH_ENABLED=true
CLIENT_ORIGIN=http://localhost:5173
