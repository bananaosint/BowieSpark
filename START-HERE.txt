FIT & CLUB SCHEDULING PLATFORM
Getting it running on your computer
===================================================================

You need Node.js 20 or newer. That is the only thing to install --
there is no database server to set up.

  Check what you have:   node --version
  Get it if you need it: https://nodejs.org   (choose the LTS build)

Open a terminal IN THIS FOLDER, then run these four commands in order:

  npm install          installs dependencies (takes a couple of minutes)
  npm run db:migrate   creates the local database file
  npm run db:seed      fills it with fake demo data
  npm run dev          starts the app

Then open http://localhost:5173 in your browser.

Press Ctrl+C in the terminal to stop it.

-------------------------------------------------------------------
SIGNING IN

Every demo account uses the password:   FitBeta2026!

  anguyen@stu.austinisd.org ...... a student: browse and sign up
  bfitzgerald@stu.austinisd.org .. a student with a locked,
                                   teacher-assigned day
  cowlin@austinisd.org ........... a teacher: sessions, roster,
                                   attendance, assigning students
  fit.admin@austinisd.org ........ an admin: settings and analytics

-------------------------------------------------------------------
DEV VIEW

The striped red bar at the top switches between "user view" (real
sign-in, the way students will actually use it) and "dev view",
which lets you hop between the demo accounts without a password.

Dev view is handy for checking how each role sees things. It only
works because this copy is running in development mode, and it
disappears completely when the app is deployed for real.

-------------------------------------------------------------------
IMPORTANT

All the data in here is INVENTED. There are no real students, and
no real student information is stored anywhere.

The accounts above all share one published password, so this must
NOT be put on the public internet as-is.

-------------------------------------------------------------------
TROUBLESHOOTING

"npm is not recognized"
    Node.js isn't installed, or the terminal was open before you
    installed it. Close the terminal, open a new one, try again.

"Port 4000 / 5173 already in use"
    Something else is using those ports, or a previous run is still
    going. Close the other terminal window and retry.

Sign-in says the account is not active
    That is correct for staff accounts you create yourself -- an
    admin has to approve them first. Sign in as the admin above,
    go to Admin > Accounts, and activate it.

Something looks broken or the data got into a weird state
    npm run db:reset     wipes the database and reloads the fake data

-------------------------------------------------------------------
README.md has the full technical detail: what is built, what is
not, and why certain decisions were made.
